//your JS code here. If required.
const firstno=parseInt(prompt("Enter first number"));
const multipliedno=firstno*2;

const secondno=parseInt(prompt("Enter second no."));

const finalresult=multipliedno+secondno;

alert("The final result is: "+finalresult);